<?xml version="1.0" encoding="utf-8"?>

<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/NAVDRAWER_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The main content view -->
    <fragment
        android:tag="LocationBrowser"
        android:name="com.darkrockstudios.apps.tminus.fragments.LocationBrowserFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".LocationBrowserActivity"
        tools:layout="@layout/fragment_location_browser"/>

    <!-- The navigation drawer -->
    <ListView
        android:id="@+id/NAVDRAWER_left_drawer"
        android:layout_width="@dimen/nav_list_width"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:background="@color/nav_drawer_background"/>
</android.support.v4.widget.DrawerLayout>