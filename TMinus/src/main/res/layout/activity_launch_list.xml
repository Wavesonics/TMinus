<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                xmlns:tminus="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

    <fragment
            android:id="@+id/launch_list"
            android:name="com.darkrockstudios.apps.tminus.fragments.LaunchListFragment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@+id/LAUNCHLIST_next_launch_container"
            android:layout_alignParentTop="true"
            tools:context=".LaunchListActivity"
            tools:layout="@android:layout/list_content"/>

</RelativeLayout>